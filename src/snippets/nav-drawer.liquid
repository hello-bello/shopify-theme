<div class="drawer left bgc-cornflower-lilac" id="nav-drawer">
  <ul class="menu vertical">
    {% for link in linklists.main-menu.links %}
      <li>
        <a href="{{ link.url }}">{{ link.title }}</a>
      </li>
    {% endfor %}
  </ul>
  <a href="#" data-close-drawer="nav-drawer">Close</a>
</div>