{%- assign suffix = color | append: '.svg' -%}

{%- if url contains "instagram" -%}
  {%- assign icon_path = 'instagram-' | append: suffix -%}
{%- elsif url contains 'facebook' -%}
  {%- assign icon_path = 'facebook-' | append: suffix -%}
{%- elsif url contains 'youtube' -%}
  {%- assign icon_path = 'youtube-' | append: suffix -%}
{%- endif -%}

{{ icon_path | asset_url | img_tag }}